# Diagramix 🎨

**Diagramix** — современный редактор диаграмм схем баз данных, написанный на Rust с использованием фреймворка Leptos и WebAssembly.

![Leptos](https://img.shields.io/badge/Leptos-0.8.0-orange)
![Rust](https://img.shields.io/badge/Rust-1.70+-blue)
![License](https://img.shields.io/badge/License-MIT-green)

## 🚀 Возможности

- ✅ **Drag & Drop** — перетаскивание таблиц на канвасе
- ✅ **Визуализация связей** — автоматическая отрисовка связей между таблицами
- ✅ **Реактивность** — мгновенное обновление при изменениях
- ✅ **SSR + Hydration** — быстрая загрузка и полная интерактивность
- ✅ **Full-stack Rust** — клиент и сервер на одном языке
- 🔄 **В разработке**: экспорт в SQL, сохранение схем, режим редактирования

## 🛠 Технологический стек

- **Frontend**: Leptos 0.8 + WASM + Tailwind CSS
- **Backend**: Axum + Tokio
- **Graph**: petgraph 0.8.3
- **Styling**: Tailwind CSS 3.4

## 📦 Установка

### Предварительные требования

1. **Rust** (stable):
```bash
rustup toolchain install stable
rustup default stable
```

2. **WASM target**:
```bash
rustup target add wasm32-unknown-unknown
```

3. **cargo-leptos**:
```bash
cargo install cargo-leptos --locked
```

4. **Node.js & npm** (для Tailwind CSS):
```bash
# Убедитесь, что установлен Node.js 18+
node --version
npm --version
```

### Клонирование и запуск

```bash
# 1. Клонируйте репозиторий
cd diagramix

# 2. Установите npm зависимости
npm install

# 3. Скомпилируйте Tailwind CSS
npm run build:css

# 4. Запустите dev-сервер
cargo leptos watch
```

Откройте браузер: **http://127.0.0.1:3000**

## 🎯 Использование

### Демо-данные

При запуске автоматически загружается демо-схема с тремя таблицами:
- **users** — таблица пользователей
- **posts** — таблица постов
- **comments** — таблица комментариев

### Интерфейс

- **Перетаскивание**: Зажмите таблицу и перетащите её
- **Связи**: Автоматически отображаются стрелками между таблицами
- **Типы связей**: 1:1, 1:N, N:M отображаются на связях

## 📂 Структура проекта

```
diagramix/
├── src/
│   ├── app.rs              # Главный компонент приложения
│   ├── main.rs             # Серверная точка входа (SSR)
│   ├── lib.rs              # Библиотечная точка входа
│   ├── core/               # Бизнес-логика и модели данных
│   │   ├── mod.rs
│   │   └── schema.rs       # Модели схемы БД (TableNode, Relationship, SchemaGraph)
│   └── ui/                 # UI компоненты
│       ├── mod.rs
│       ├── canvas.rs       # Холст редактора с drag-drop
│       └── table.rs        # Компонент таблицы
├── style/
│   ├── input.css           # Tailwind CSS (исходный)
│   └── output.css          # Скомпилированный CSS
├── public/                 # Статические файлы
│   └── favicon.ico
├── Cargo.toml              # Rust зависимости
├── package.json            # NPM зависимости (только Tailwind)
└── tailwind.config.js      # Конфигурация Tailwind
```

### Архитектура модулей

- **`core/`** — основная бизнес-логика, модели данных, не зависит от UI
  - `schema.rs` — структуры данных для таблиц, колонок, связей и графа схемы
  
- **`ui/`** — Leptos компоненты для визуализации
  - `canvas.rs` — основной канвас с drag & drop и отрисовкой связей
  - `table.rs` — визуальное представление таблицы БД

## 🧪 Разработка

### Запуск с hot-reload

```bash
# Терминал 1: Запуск Leptos dev-сервера
cargo leptos watch

# Терминал 2 (опционально): Watch режим для Tailwind
npm run watch:css
```

### Проверка кода

```bash
# Проверить серверную версию
cargo check --features ssr --no-default-features

# Проверить клиентскую версию (WASM)
cargo check --lib --features hydrate --target wasm32-unknown-unknown --no-default-features

# Проверить всё
cargo check
```

### Сборка для production

```bash
# 1. Скомпилировать Tailwind
npm run build:css

# 2. Собрать проект
cargo leptos build --release
```

Готовые файлы будут в:
- Сервер: `target/server/release/diagramix`
- WASM + JS: `target/site/pkg/`

## 📚 API и расширение

### Создание своей схемы

```rust
use diagramix::core::*;

let mut graph = SchemaGraph::new();

// Создать таблицу
let users = graph.add_node(
    TableNode::new("users")
        .with_position(100.0, 100.0)
        .add_column(Column::new("id", "INTEGER").primary_key())
        .add_column(Column::new("name", "VARCHAR(255)").not_null())
);

let posts = graph.add_node(
    TableNode::new("posts")
        .with_position(400.0, 100.0)
        .add_column(Column::new("id", "INTEGER").primary_key())
        .add_column(Column::new("user_id", "INTEGER").not_null())
);

// Создать связь
graph.add_edge(
    users,
    posts,
    Relationship::new(
        "user_posts",
        RelationshipType::OneToMany,
        "id",
        "user_id",
    ),
);
```

### Добавление новых компонентов

Компоненты находятся в `src/ui/`. Чтобы добавить новый:

1. Создайте файл `src/ui/my_component.rs`
2. Определите компонент с макросом `#[component]`
3. Экспортируйте в `src/ui/mod.rs`
4. Используйте в `src/app.rs`

## 🔧 Зависимости

### Rust (Cargo.toml)
- `leptos` 0.8.0 — реактивный фреймворк
- `leptos_meta` 0.8.0 — управление meta-тегами
- `axum` 0.8.0 — веб-сервер (SSR)
- `tokio` — async runtime
- `petgraph` 0.8.3 — библиотека графов
- `serde` — сериализация данных
- `wasm-bindgen` — WASM биндинги

### NPM (package.json)
- `tailwindcss` 3.4.18 — утилитарный CSS

## 🤝 Вклад в проект

Приветствуются любые вклады! Для этого:

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 TODO

- [ ] Панель инструментов для добавления таблиц
- [ ] Редактирование колонок через UI
- [ ] Экспорт в SQL (PostgreSQL, MySQL, SQLite)
- [ ] Сохранение схем (localStorage/IndexedDB)
- [ ] Зум и пан холста
- [ ] Undo/Redo
- [ ] Множественный выбор
- [ ] Темная тема
- [ ] Экспорт в PNG/SVG
- [ ] Импорт из SQL/DDL

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для деталей.

## 🙏 Благодарности

- [Leptos](https://github.com/leptos-rs/leptos) — реактивный фреймворк на Rust
- [petgraph](https://github.com/petgraph/petgraph) — библиотека графов
- [Tailwind CSS](https://tailwindcss.com) — утилитарный CSS
- [Axum](https://github.com/tokio-rs/axum) — веб-фреймворк

---

**Сделано с ❤️ на Rust + WebAssembly**