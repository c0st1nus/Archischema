# Changelog

Все значимые изменения в проекте Diagramix документируются в этом файле.

## [Unreleased]

### Рефакторинг архитектуры - 2024

Проведена полная реорганизация проекта для создания более чистой и maintainable кодовой базы.

#### ✨ Добавлено

- **Новая структура модулей:**
  - `src/core/` — бизнес-логика и модели данных
  - `src/ui/` — UI компоненты
  
- **Документация:**
  - `ARCHITECTURE.md` — подробное описание архитектуры проекта
  - `QUICKSTART.md` — краткое руководство для быстрого старта
  - `CHANGELOG.md` — история изменений

#### 🔄 Изменено

- **Структура директорий:**
  - `src/models/` → `src/core/` (переименовано для ясности)
  - `src/components/` → `src/ui/` (более точное название)
  - `src/models/mod.rs` → `src/core/schema.rs` (разделение по доменам)

- **Упрощение приложения:**
  - Убран роутинг (приложение на одну страницу)
  - Упрощён `app.rs` — нет необходимости в `Router` и `Routes`

- **Модели данных:**
  - Убраны UUID из `TableNode` и `Relationship` (используются индексы графа)
  - Более простая и эффективная идентификация узлов

- **Cargo.toml:**
  - Изменена edition с `2024` на `2021` (стабильная версия)
  - Упрощены комментарии

- **package.json:**
  - Более точное описание проекта
  - Правильная лицензия (MIT)
  - Добавлены keywords

#### ❌ Удалено

- **Зависимости Rust:**
  - `leptos_router` — не нужен для single-page приложения
  - `leptos-use` — не использовался в проекте
  - `uuid` — заменён на индексы графа (NodeIndex)

- **NPM зависимости:**
  - `autoprefixer` — избыточно для современных браузеров
  - `postcss` — не используется без дополнительных плагинов

- **Файлы и директории:**
  - `postcss.config.js` — больше не нужен
  - `style/main.scss` — избыточная абстракция (просто импортировал output.css)
  - `end2end/` — директория с E2E тестами (устаревший пример)

- **Конфигурация:**
  - Удалены настройки end2end тестов из `Cargo.toml`

#### 🐛 Исправлено

- Корректные импорты во всех модулях после реорганизации
- Правильная видимость экспортов в `ui/mod.rs`

#### 📝 Документация

- Полностью обновлён `README.md`:
  - Актуальная структура проекта
  - Обновлённые инструкции по установке
  - Список зависимостей с обоснованием
  - Убраны упоминания удалённых инструментов (SCSS, E2E тесты)

- Добавлен `ARCHITECTURE.md`:
  - Подробное описание архитектуры
  - Диаграммы взаимодействия компонентов
  - Объяснение выбора зависимостей
  - Планы на будущее

- Добавлен `QUICKSTART.md`:
  - Пошаговая инструкция для быстрого старта
  - Решение типичных проблем
  - Справочник команд

#### 🎯 Мотивация

Основные причины рефакторинга:

1. **Упрощение:** Убраны неиспользуемые зависимости и файлы
2. **Ясность:** Чёткое разделение на бизнес-логику (core) и UI
3. **Maintainability:** Проще понять структуру и добавлять новый функционал
4. **Производительность:** Меньше зависимостей = быстрее компиляция и меньше размер бандла

#### 📊 Метрики

- **Зависимости Rust:** 12 → 8 (-33%)
- **Зависимости NPM:** 3 → 1 (-66%)
- **Файлы конфигурации:** 5 → 3 (-40%)
- **Директорий в src/:** 2 → 2 (без изменений, но переименованы)

### Миграция

Если вы использовали предыдущую версию:

#### Импорты

```rust
// Было:
use diagramix::models::*;
use diagramix::components::*;

// Стало:
use diagramix::core::*;
use diagramix::ui::*;
```

#### Модели

```rust
// Было:
let node = TableNode {
    id: Uuid::new_v4(),  // UUID генерировался вручную
    name: "users".into(),
    // ...
};

// Стало:
let node = TableNode::new("users")  // ID не нужен - используется NodeIndex
    .add_column(Column::new("id", "INTEGER").primary_key());
```

#### Роутинг

```rust
// Было:
<Router>
    <Routes>
        <Route path=StaticSegment("") view=HomePage/>
    </Routes>
</Router>

// Стало:
<SchemaCanvas graph=graph />  // Прямой рендеринг, без роутера
```

---

## [0.1.0] - Initial Release

### Добавлено

- Базовая функциональность редактора диаграмм
- Drag & Drop для таблиц
- Визуализация связей между таблицами
- SSR + Hydration с Leptos
- Демо-схема с тремя таблицами (users, posts, comments)
- Интеграция с Tailwind CSS
- Поддержка petgraph для представления схемы

---

[Unreleased]: https://github.com/yourusername/diagramix/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/yourusername/diagramix/releases/tag/v0.1.0